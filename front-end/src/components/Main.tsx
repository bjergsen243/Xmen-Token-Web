
export type Token = {
    address: string
    name: string
}

export const Main = () => {
    // show token values from the wallet

    // get the address of different tokens
    // get the balance of the users waller
    // => sending brownie config to our "src" folder
    // send the build folder
    const xmenTokenAddress = 0x534Eb14712bE0730af161Bc746E984487bD25102;
    
}